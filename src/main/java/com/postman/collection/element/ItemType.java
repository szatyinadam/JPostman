package com.postman.collection.element;
/**
 * 
 * 
 * Enumerates the two types of objects that can serve as an element in a collections <code>item</code> array.  A folder is an item that does not have a request.
 * 
 * 
 */
public enum ItemType {
    FOLDER,
    REQUEST
}
